## [1.0.0] - 2025-12-23

### Added
- Frontend: configuration environments (`environment.ts`, `environment.prod.ts`).
- Frontend: `nginx.conf` file and proxy integration to backend.

### Changed
- Backend: version update in `pyproject.toml`.
- Frontend: version update in `package.json`.
- Backend: refactored the return of the file upload endpoint.

### Fixed
- Frontend: handling of potential errors from the API.